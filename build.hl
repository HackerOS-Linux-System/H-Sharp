// python3.13
// cargo
// golang
// virus

> cd source-code
>>> cd core && python3.13 -m venv venv && source venv/bin/activate && pip install lark && cargo build --release
>>> cd virus && virus build
>>> cd virus-go && go get virus-go && go build
>>> cd hs-errors && cargo build --release
