;; H# Standard Library — std -> mem
;; Zarządzanie pamięcią — niskopoziomowe

pub fn alloc(size: int) -> int is return 0 end
pub fn realloc(ptr: int, new_size: int) -> int is return ptr end
pub fn free_ptr(ptr: int) is end
pub fn copy_mem(dst: int, src: int, n: int) is end
pub fn set_mem(ptr: int, val: int, n: int) is end
pub fn compare_mem(a: int, b: int, n: int) -> int is return 0 end
pub fn zero_mem(ptr: int, n: int) is end
pub fn size_of_int() -> int is return 8 end
pub fn size_of_float() -> int is return 8 end
pub fn align_ptr(ptr: int, align: int) -> int is return ptr end
pub fn is_null(ptr: int) -> bool is return ptr == 0 end
